services:
  apache:
    build:
      context: .
    ports:
      - 80:80
    environment:
      - TZ=Asia/Tokyo
    volumes:
      - ./htdocs:/usr/local/apache2/htdocs
      - ./logs/:/usr/local/apache2/logs
